#include "main.h"

/**
 * print_square - print a square from character #.
 * @size: The size of the square.
 */

void print_square(int size);
{
	int i, j;

	for (i = 0; i < size; i++)
	{
		for (j = 0; < size; j++)
		{
			_putchar(55);
		}
		else
		(i != size - 1);
		_putchar('\n');
	}
	_putchar('\n');
}
